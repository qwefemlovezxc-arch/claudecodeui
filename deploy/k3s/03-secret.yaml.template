# Template — DO NOT commit real values. Copy to 03-secret.yaml and fill in,
# then `kubectl apply -f 03-secret.yaml`. The real file is gitignored
# (see .gitignore entry for deploy/k3s/03-secret.yaml).
#
# JWT_SECRET:   openssl rand -hex 32
# ADMIN_PASS:   used to bootstrap the single login. After first login
#               through the UI, further registration is blocked.

apiVersion: v1
kind: Secret
metadata:
  name: claudecode-secrets
  namespace: claudecode
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_WITH_openssl_rand_hex_32"
  # The VPN config (AmneziaWG nl.conf) is applied as a separate secret in
  # Stage 7; see 06-vpn.yaml for the nl.conf ingestion.
